apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: test

resources:
  - ../../base

patches:
  - path: replicas-patch.yaml
# configMapGenerator:
#   - name: xdatabase-proxy-config
#     behavior: merge
